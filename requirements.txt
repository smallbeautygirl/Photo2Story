google-genai>=0.8.0
python-dotenv>=1.0.0
scikit-learn>=1.4.0
Pillow>=10.3.0
reportlab>=4.2.0
gradio>=4.26.0
pyyaml>=6.0.1
numpy>=1.26.0
pytest>=8.1.0
pytest-mock>=3.14.0
piexif>=1.1.3
fal-client>=1.0.0
pypdf>=4.2.0  # test-only

# GPU / local-model dependencies (torch, diffusers, etc.) live in
# requirements-gpu.txt — install with `pip install -r requirements-gpu.txt`
# only when running local model inference.
