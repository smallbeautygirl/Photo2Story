google-genai>=0.8.0
python-dotenv>=1.0.0
scikit-learn>=1.4.0
Pillow>=10.3.0
pillow-heif>=0.18.0
reportlab>=4.2.0
gradio>=4.26.0
pyyaml>=6.0.1
numpy>=1.26.0
pytest>=8.1.0
pytest-mock>=3.14.0
piexif>=1.1.3
pypinyin>=0.55.0
fal-client>=1.0.0
pypdf>=4.2.0  # test-only

# Local-model dependencies (torch, diffusers, etc.) live in two optional files:
#   requirements-clip.txt — CLIP-only, CPU-friendly (stage0 hybrid/clip_only mode,
#                           eval/clip_score.py)
#   requirements-gpu.txt  — local Stable Diffusion backend (stage2_illustrate.py,
#                           model_manager.py), needs a GPU for practical speed
# Not required when using only API-based backends (google-genai, fal-client)
# with stage0.mode: llm_only or random.
