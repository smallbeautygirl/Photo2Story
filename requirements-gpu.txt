# Local Stable Diffusion backend dependencies, optional. Needs a GPU for
# practical inference speed.
# Install with: pip install -r requirements-gpu.txt
# Needed for: stage2_illustrate.py's local backend, model_manager.py.
# Not required when stage2.backend is fal (API-based), or when only using
# stage0's CLIP clustering — see requirements-clip.txt for that.

torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0,<5.0
accelerate>=0.30.0,<2.0
diffusers>=0.27.0
ip-adapter>=0.1.0
